## 飞书多维表格「周期环比分析」插件需求文档

### 1. 背景与目标

- **背景**
  - 业务当前在飞书多维表格（Bitable）中，已经有一套完整的数据模型和透视表，用来统计某些指标（如 GMV、销量、推广费等）。
  - 在该多维表格中，已经存在两个关键组件：
    - **时间周期选择组件**（如下拉选择「周期」）；
    - **透视表组件**（按「通用名 × 月份」等维度汇总的统计表）。
  - 目前，业务在更换时间周期后，只能在透视表中看到各期的数值，但缺乏清晰直观的「环比」分析视图。

- **目标**
  - 开发一个**新的飞书多维表格 Dashboard 插件组件**：
    - 能够监听/获取「时间周期选择组件」的当前选择；
    - 根据所选时间周期，到透视表对应的数据中读取数值；
    - 在插件中计算并展示指定指标的**环比数据（Period over Period, 环比）**，帮助业务快速判断趋势。

### 2. 运行环境与集成对象

- **宿主环境**
  - 飞书多维表格（Bitable），已有一个 Base，地址为：
    - `https://ewzcvajvaha.feishu.cn/wiki/A8FNwNs9ii9hOzkq5skc5JHznPh?base_hp_from=workbench&table=blkxKzcE20Uin4p5`
  - 插件以 Dashboard 形式挂载在该 Base 中。

- **现有关键组件**
  - **时间周期选择组件**
    - 表现形式：一个下拉框，展示「周期」字段，可选择具体的时间周期值（如某个月份、某阶段）。
    - 期望：插件能够根据用户在此组件中的选择，获取当前所选周期值（例如 `2025-07` 等）。
  - **透视表组件**
    - 表现形式：类似透视表/数据透视图，列包含：
      - 列头为时间周期（如：`2025-07`、`2025-08`、`2025-09`...以及`合计`）；
      - 行维度为「通用名」（以及其它可能的维度，如品牌、规格等）。
      - 单元格为某个统计指标（如销量、GMV 等）的汇总值。
    - 用户在透视表中点击某一单元格，可以弹出**数据详情**视图，展示该指标对应的明细记录（如：日期、店铺、通用名、规格、结算价、销售金额、推广费等列），参考截图与记录示例：
      - `https://ewzcvajvaha.feishu.cn/record/Oz8rr1OtseFyfHchjw5cRa5En3g`

### 3. 新插件功能需求

#### 3.1 功能概述

- 在 Dashboard 中新增一个组件（下称「环比分析组件」），主要能力：
  1. **根据时间周期选择组件的选择结果，自动更新自身分析周期**；
  2. **从透视表或其底层数据表中获取相应周期的数据**；
  3. **基于当前周期与对比周期（上一周期）的数值，计算环比**；
  4. **以图表/列表的形式展示不同通用名在所选周期的数值与环比情况**。

#### 3.2 时间周期逻辑

- **周期来源**
  - 直接复用现有的「时间周期选择组件」的结果，保证用户只需要在一个地方选择周期。
- **周期类型**
  - 初版默认按**月份**为单位（例如：`2025-07`、`2025-08`），后续可扩展为周、日等。
- **环比定义**
  - 当前周期 = 用户在周期组件中选中的周期（例如 `2025-08`）。
  - 对比周期 = 当前周期的上一周期（例如 `2025-07`）。
  - 环比计算：  
    \[
    环比 = \frac{当前周期值 - 对比周期值}{对比周期值} \times 100\%
    \]
  - 若对比周期无数据或为 0：
    - 默认处理方式：展示为 `--` 或 `N/A`，并标记为无可比数据。

#### 3.3 数据来源与映射

- **数据来源**
  - 以透视表对应的底层数据表/视图为准。
  - 字段示例（结合截图与记录示例）：
    - 日期（如 `2025-07-31`）
    - 月份（如 `2025-07`）
    - 店铺
    - 系统 ID
    - 类型
    - 品牌
    - 通用名
    - 规格
    - 结算价、供货价
    - 销售额 GMV、退货金额、盒数、公司收入、推广费、推广成交额、运费、平台扣点、配送费、返利等
  - 环比分析所需的**核心维度**与**指标**需在后续进一步确认：
    - 维度：至少包含「通用名」，也可以扩展到品牌、店铺等；
    - 指标：首版可选 1～3 个关键指标（如「销售额 GMV」「盒数」「推广费」）。

- **数据聚合规则**
  - 在插件内部需要按照与透视表一致的规则进行聚合，以保证数值对齐：
    - 聚合粒度：按「通用名 + 周期」汇总；
    - 聚合函数：默认为**求和**（Sum）。
  - 插件可直接复用透视表的汇总结果，或者从明细表自行聚合（具体取决于 JS-SDK 暴露的数据接口能力，后续设计阶段细化）。

#### 3.4 展示形式

- **整体布局**
  - 插件主体区域展示一个列表或图表，支持查看：
    - 通用名；
    - 当前周期值；
    - 上一周期值；
    - 环比百分比及涨跌方向。

- **推荐展示形态**
  - 表格形式（首版）：
    - 列示例：
      - 通用名
      - 当前周期（如 `2025-08`）数值
      - 上一周期（如 `2025-07`）数值
      - 环比（百分比，带颜色/箭头）
  - 后续可考虑：
    - 条形图/折线图：展示 Top N 通用名的走势与环比。

- **交互需求**
  - 选择周期变更时，环比分析组件自动刷新数据；
  - 列表行可支持简单排序（按当前值/环比排序）；
  - 可考虑在 hover 时展示精确数值和额外说明（如实际值、单位、环比公式）。

### 4. 配置与可扩展性需求

- **配置入口**
  - 通过 Dashboard 插件的「配置模式」，提供简单配置项：
    - 选择**数据来源表/视图**（如：销售明细表）；
    - 选择**透视表中使用的时间字段**（如：月份字段）；
    - 选择**分析维度字段**（默认通用名）；
    - 选择**指标字段**（如销售额 GMV、盒数、推广费等）。


### 5. 异常与边界情况

- **无数据**
  - 当当前周期或上一周期无数据时：
    - 行仍显示维度名称，但数值为 0 或 `--`；
    - 环比显示为 `--` 或 `N/A`，并在 tooltip 中提示「无可比数据」。

- **数据延迟或加载失败**
  - 若通过 JS-SDK 拉取数据失败：
    - 显示统一错误提示（可引导用户刷新页面或联系管理员）；
    - 控件不崩溃，保持基础布局。

- **多周期快速切换**
  - 用户在周期组件中快速切换多次时：
    - 插件需要做请求/计算节流或最新结果优先，避免界面频繁抖动；
    - 确保最终展示的是**最近一次选择**对应的数据。




